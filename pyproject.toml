[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "airflow-data-pipeline-demo"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["scripts*"]